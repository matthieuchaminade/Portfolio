import type { CSSProperties } from "react";

/** Shared typography for “Work Samples” and each project title (PP Neue Machina Inktrap Light). */
export const PROJECT_HEADING_STYLE: CSSProperties = {
  color: "#B9B9BB",
  fontSize: "clamp(2rem, 7vw, 3.75rem)",
  lineHeight: "1.05",
  fontWeight: 300,
};

export const PROJECT_HEADING_CLASS =
  "font-ppNeueMachinaInktrapLight tracking-tight uppercase";
